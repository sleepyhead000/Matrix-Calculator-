# Matrix Calculator

Simple browser-based matrix calculator that shows full step-by-step solutions.

## Run locally

1. Open `index.html` in any browser.
2. Or open `main.htm` (legacy entry file kept for compatibility).
